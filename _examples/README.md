# Examples

These files demonstrate the full ingest pipeline: raw article → source page → concept page → entity page.

The wikilinks in these examples (e.g. `[[Deep-Learning-Factor-Extraction]]`) intentionally point to pages that don't exist — they represent pages that would be created in a real wiki during the ingest of additional sources.
